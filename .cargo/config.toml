[alias]
cov-unit = "llvm-cov --workspace --lib --bins --locked --fail-under-lines 83.5"
cov-combined = "llvm-cov --workspace --tests --locked --fail-under-lines 86.0 -- --skip every_shipped_scenario_stays_valid_and_live"
