[alias]
cov-unit = "llvm-cov --workspace --lib --bins --locked --fail-under-lines 83.5"
cov-combined = "llvm-cov --workspace --tests --locked --fail-under-lines 86.0 -- --test-threads=1 --skip representative_scenarios_stay_valid_and_live --skip scripted_controller_matches_hash_fixtures --skip balanced_mirror_stays_active_without_rejected_commands --skip distinct_balanced_personalities_play_decisive_matches_in_either_seat --skip dense_connected_target_trains_and_freezes_the_selected_bomber_roster"
